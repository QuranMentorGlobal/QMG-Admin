# ── Muddarris .gitignore (add to the ROOT of BOTH repos: qmg-frontend and qmg-admin) ──

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js build output
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# env / secrets — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# vercel
.vercel

# logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# misc / OS
.DS_Store
*.pem
coverage/
